
Based on Langchain 1.0 and the Alibaba Bailian platform,
Demo of dynamically loading agents through LLM
Solved the problem of writing a dead tools [] array in the code when loading agents

#Need to modify OPENAI_SPI_KEY in the. env file before running
python llm.py


基于langchain1.0, 以及阿里百炼平台，
通过LLM动态加载agents的演示demo
解决了加载agents时要在代码里写死tools[]数组

#需要修改.env文件里的OPENAI_API_KEY后运行
python llm.py